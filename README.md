Display Character Sequence
==========================

A simple Python script to make the characters appear one after the other on the terminal. You can specify the delay and the text input on the command line. Usage is as given below:

	python displayText.py 0.2 PrintMe